# docker-compose.yml 
version: '3.8'

services:
    testci:
      image: jedis/testci:latest
      container_name: testci
      restart: unless-stopped
      ports:
        - 8081:8080

volumes:
  octagon-db:
